"use client";

import React from "react";
import { motion } from "framer-motion";
import { StatusDot } from "./Logo";
import { AnimatedSection } from "./Animations";

// Testimonials section has been removed.
// Social proof is now handled via the "1,000+ conversations analyzed" stat
// in the hero section and the trust bar in pricing.
export function Testimonials() {
  return null;
}
